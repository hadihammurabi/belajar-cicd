language: node_js
install:
  - sh ./script/install.sh

branches:
  only:
  - master

jobs:
  include:
    - stage: test
      script: sh ./script/test.sh
    - stage: deploy
      script: sh ./script/deploy.sh
